# [[secrets.*]] entry schema — each secret declaration

[meta]
version = "v1"

[fields.value_source]
type = "string"
required = true

[fields.targets]
type = "list<string>"
required = true
enum_items = ["compose", "file", "vault", "k8s"]

[fields.options]
type = "record"
required = false
default = {}
